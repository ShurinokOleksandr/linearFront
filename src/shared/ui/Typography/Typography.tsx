import { CommonProps } from '@/shared/ui/CommonProps/CommonProps';
import styled from 'styled-components';
import * as CSS from 'csstype';


export const Typography = styled(CommonProps).attrs({ as: 'p' })<CSS.Properties>`

`;