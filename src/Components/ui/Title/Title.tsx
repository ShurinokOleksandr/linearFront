import { CommonProps } from '@/Components/ui/CommonProps/CommonProps';
import styled from 'styled-components';
import * as CSS from 'csstype';

 
export const Title =styled(CommonProps).attrs({ as: 'p' })<CSS.Properties>`

`;

